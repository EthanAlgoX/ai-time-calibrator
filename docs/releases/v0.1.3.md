# AI Time Calibrator v0.1.3

Skill packaging release.

## Highlights

- Moves the Codex-specific skill path to the standard `skills/ai-time-calibrator/SKILL.md` layout.
- Adds `.codex-plugin/plugin.json`, `.claude-plugin/plugin.json`, and `.cursor-plugin/plugin.json`.
- Adds `.agents/skills` compatibility symlink.
- Removes the non-standard Claude `CLAUDE.md` adapter in favor of the shared `SKILL.md`.
- Updates README installation guidance around skill-first and plugin-style usage.

## Validation

```bash
python3 -m py_compile scripts/estimate.py
python3 -m unittest discover -s tests
```
